The program is in C. It is an implementation of a simple shell interpreter. It can run things like ls, cd, ect. and can run things in the background using bg before the command (eg. bg ls).
sample command input$ make
sample command input$ ./a.out